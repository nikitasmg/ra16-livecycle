import styled from "styled-components";

const Card = styled.div`
  position: relative;
  width: calc(25% - 40px);
  padding: 10px 20px;
  border: 2px solid black;
  
`

export const Styled = {
    Card
}